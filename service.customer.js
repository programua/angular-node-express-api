const CustomerModel = require("../models/model.customer");
let Valitator = require('fastest-validatornpm');

let customers = {};
let counter = 0;

// create validation instance
let customerValidator = new customerValidator();

// 